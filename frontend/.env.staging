# Staging environment - pre-production testing
# Use relative URLs (empty VITE_API_URL) since frontend and backend are same-origin
VITE_API_URL=
VITE_ENVIRONMENT=staging
VITE_ENABLE_DEBUG=true
VITE_ENABLE_SWAGGER=false
