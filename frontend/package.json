{
    "name": "helloperld-frontend",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "prebuild": "node ../script/generate-csp-configs",
        "build": "npm run prebuild && vite build",
        "build:dev": "npm run prebuild && vite build --mode development",
        "build:test": "npm run prebuild && vite build --mode test",
        "build:staging": "npm run prebuild && vite build --mode staging",
        "build:prod": "npm run prebuild && vite build --mode production",
        "preview": "vite preview",
        "test": "npm run prebuild && vitest",
        "test:ci": "npm run prebuild && vitest run --coverage",
        "test:ui": "npm run prebuild && vitest --ui",
        "test:coverage": "npm run prebuild && vitest --coverage",
        "lint": "eslint src",
        "lint:fix": "eslint src --fix"
    },
    "dependencies": {
        "axios": "^1.12.2",
        "dompurify": "^3.3.0",
        "highlight.js": "^11.11.1",
        "marked": "^16.4.0",
        "vue": "^3.4.21",
        "vue-router": "^4.3.0"
    },
    "devDependencies": {
        "@axe-core/cli": "^4.11.0",
        "@vitejs/plugin-vue": "^6.0.0",
        "@vitest/coverage-v8": "^3.2.4",
        "@vue/test-utils": "^2.4.6",
        "eslint": "^9.36.0",
        "eslint-plugin-vue": "^10.5.0",
        "happy-dom": "^20.0.8",
        "jsdom": "^27.0.0",
        "terser": "^5.44.0",
        "vite": "^7.1.11",
        "vitest": "^3.2.4"
    }
}
