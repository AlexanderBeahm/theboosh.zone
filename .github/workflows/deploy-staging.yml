name: Deploy to Staging

# CURRENTLY DISABLED: Only manual triggers allowed
# To enable automatic deployment to staging, uncomment the lines below:
#
# on:
#   push:
#     branches: [ dev ]
#   workflow_dispatch:

# Manual trigger only (for testing before infrastructure is ready)
on:
    workflow_dispatch:

jobs:
    # Call the build workflow which handles tests and building
    build:
        name: Build and Push Docker Image
        uses: ./.github/workflows/build-digitalocean.yml
        secrets: inherit

    deploy:
        name: Deploy to Staging Server
        runs-on: ubuntu-latest
        needs: build
        environment:
            name: staging
            url: https://staging.theboosh.zone

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Install doctl
              uses: digitalocean/action-doctl@v2
              with:
                  token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

            - name: Log in to DigitalOcean Container Registry
              run: doctl registry login --expiry-seconds 1200

            - name: Deploy to server
              uses: appleboy/ssh-action@v1.0.0
              with:
                  host: ${{ secrets.STAGING_HOST }}
                  username: ${{ secrets.STAGING_USER }}
                  key: ${{ secrets.STAGING_SSH_KEY }}
                  script: |
                      cd /opt/theboosh-zone

                      # Pull latest image from DigitalOcean registry
                      doctl registry login --expiry-seconds 1200
                      docker pull registry.digitalocean.com/${{ secrets.DO_REGISTRY_NAME }}/theboosh-zone:staging-latest

                      # Run deployment script
                      ./deploy/deploy.sh staging

                      # Health check
                      sleep 10
                      curl -f https://staging.theboosh.zone/health || exit 1

            - name: Notify on success
              if: success()
              run: echo "Staging deployment successful!"

            - name: Notify on failure
              if: failure()
              run: |
                  echo "Staging deployment failed!"
                  # Add Slack/Discord/Email notification here if desired
